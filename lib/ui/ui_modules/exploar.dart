import 'package:flutter/material.dart';

class Exploar extends StatelessWidget {
const Exploar({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(

    
    );
  }
}